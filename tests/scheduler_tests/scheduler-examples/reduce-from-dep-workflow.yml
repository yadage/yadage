context: {}
stages:
  - name: mystage
    dependencies:
      - init
    parameters:
      one: one_val
    scheduler:
      scheduler_type: reduce-from-dep
      step: {$ref: singleout.yml}
      from_stages:
        - init
      outputs: multiple
      to_input: two