context: {}
stages:
  - name: astep
    dependencies:
      - init
    parameters:
      one: one_val
    scheduler:
      scheduler_type: 'map-from-dep'
      step: {$ref: singleout.yml}
      from_stages:
        - init
      outputs: list_context
      to_input: two